{
  "name": "jai-sandbox",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that wraps @cloudflare/sandbox so the JAI backend can run skill scripts safely.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.10.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.94.0"
  }
}
